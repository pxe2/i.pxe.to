<<<<<<< HEAD
<<<<<<< HEAD
#!ipxe

# 01.header.distro.ipxe.erb

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Centos Releases


# 02.distro.ipxe.erb 

item 2.1 ${space} ${os} 2.1 
# 02.distro.ipxe.erb 

item 2.1 ${space} ${os} 2.1 
# 02.distro.ipxe.erb 

item 3.1 ${space} ${os} 3.1 
# 02.distro.ipxe.erb 

item 3.1 ${space} ${os} 3.1 
# 02.distro.ipxe.erb 

item 3.3 ${space} ${os} 3.3 
# 02.distro.ipxe.erb 

item 3.3 ${space} ${os} 3.3 
# 02.distro.ipxe.erb 

item 3.4 ${space} ${os} 3.4 
# 02.distro.ipxe.erb 

item 3.4 ${space} ${os} 3.4 
# 02.distro.ipxe.erb 

item 3.5 ${space} ${os} 3.5 
# 02.distro.ipxe.erb 

item 3.5 ${space} ${os} 3.5 
# 02.distro.ipxe.erb 

item 3.6 ${space} ${os} 3.6 
# 02.distro.ipxe.erb 

item 3.6 ${space} ${os} 3.6 
# 02.distro.ipxe.erb 

item 3.7 ${space} ${os} 3.7 
# 02.distro.ipxe.erb 

item 3.7 ${space} ${os} 3.7 
# 02.distro.ipxe.erb 

item 3.8 ${space} ${os} 3.8 
# 02.distro.ipxe.erb 

item 3.8 ${space} ${os} 3.8 
# 02.distro.ipxe.erb 

item 3.9 ${space} ${os} 3.9 
# 02.distro.ipxe.erb 

item 3.9 ${space} ${os} 3.9 
# 02.distro.ipxe.erb 

item 4.0 ${space} ${os} 4.0 
# 02.distro.ipxe.erb 

item 4.0 ${space} ${os} 4.0 
# 02.distro.ipxe.erb 

item 4.1 ${space} ${os} 4.1 
# 02.distro.ipxe.erb 

item 4.1 ${space} ${os} 4.1 
# 02.distro.ipxe.erb 

item 4.2 ${space} ${os} 4.2 
# 02.distro.ipxe.erb 

item 4.2 ${space} ${os} 4.2 
# 02.distro.ipxe.erb 

item 4.3 ${space} ${os} 4.3 
# 02.distro.ipxe.erb 

item 4.3 ${space} ${os} 4.3 
# 02.distro.ipxe.erb 

item 4.4 ${space} ${os} 4.4 
# 02.distro.ipxe.erb 

item 4.4 ${space} ${os} 4.4 
# 02.distro.ipxe.erb 

item 4.5 ${space} ${os} 4.5 
# 02.distro.ipxe.erb 

item 4.5 ${space} ${os} 4.5 
# 02.distro.ipxe.erb 

item 4.6 ${space} ${os} 4.6 
# 02.distro.ipxe.erb 

item 4.6 ${space} ${os} 4.6 
# 02.distro.ipxe.erb 

item 4.7 ${space} ${os} 4.7 
# 02.distro.ipxe.erb 

item 4.7 ${space} ${os} 4.7 
# 02.distro.ipxe.erb 

item 4.8 ${space} ${os} 4.8 
# 02.distro.ipxe.erb 

item 4.8 ${space} ${os} 4.8 
# 02.distro.ipxe.erb 

item 4.9 ${space} ${os} 4.9 
# 02.distro.ipxe.erb 

item 4.9 ${space} ${os} 4.9 
# 02.distro.ipxe.erb 

item 5.0 ${space} ${os} 5.0 
# 02.distro.ipxe.erb 

item 5.0 ${space} ${os} 5.0 
# 02.distro.ipxe.erb 

item 5.1 ${space} ${os} 5.1 
# 02.distro.ipxe.erb 

item 5.1 ${space} ${os} 5.1 
# 02.distro.ipxe.erb 

item 5.10 ${space} ${os} 5.10 
# 02.distro.ipxe.erb 

item 5.10 ${space} ${os} 5.10 
# 02.distro.ipxe.erb 

item 5.11 ${space} ${os} 5.11 
# 02.distro.ipxe.erb 

item 5.11 ${space} ${os} 5.11 
# 02.distro.ipxe.erb 

item 5.2 ${space} ${os} 5.2 
# 02.distro.ipxe.erb 

item 5.2 ${space} ${os} 5.2 
# 02.distro.ipxe.erb 

item 5.3 ${space} ${os} 5.3 
# 02.distro.ipxe.erb 

item 5.3 ${space} ${os} 5.3 
# 02.distro.ipxe.erb 

item 5.4 ${space} ${os} 5.4 
# 02.distro.ipxe.erb 

item 5.4 ${space} ${os} 5.4 
# 02.distro.ipxe.erb 

item 5.5 ${space} ${os} 5.5 
# 02.distro.ipxe.erb 

item 5.5 ${space} ${os} 5.5 
# 02.distro.ipxe.erb 

item 5.6 ${space} ${os} 5.6 
# 02.distro.ipxe.erb 

item 5.6 ${space} ${os} 5.6 
# 02.distro.ipxe.erb 

item 5.7 ${space} ${os} 5.7 
# 02.distro.ipxe.erb 

item 5.7 ${space} ${os} 5.7 
# 02.distro.ipxe.erb 

item 5.8 ${space} ${os} 5.8 
# 02.distro.ipxe.erb 

item 5.8 ${space} ${os} 5.8 
# 02.distro.ipxe.erb 

item 5.9 ${space} ${os} 5.9 
# 02.distro.ipxe.erb 

item 5.9 ${space} ${os} 5.9 
# 02.distro.ipxe.erb 

item 6.0 ${space} ${os} 6.0 
# 02.distro.ipxe.erb 

item 6.0 ${space} ${os} 6.0 
# 02.distro.ipxe.erb 

item 6.1 ${space} ${os} 6.1 
# 02.distro.ipxe.erb 

item 6.1 ${space} ${os} 6.1 
# 02.distro.ipxe.erb 

item 6.2 ${space} ${os} 6.2 
# 02.distro.ipxe.erb 

item 6.2 ${space} ${os} 6.2 
# 02.distro.ipxe.erb 

item 6.3 ${space} ${os} 6.3 
# 02.distro.ipxe.erb 

item 6.3 ${space} ${os} 6.3 
# 02.distro.ipxe.erb 

item 6.4 ${space} ${os} 6.4 
# 02.distro.ipxe.erb 

item 6.4 ${space} ${os} 6.4 
# 02.distro.ipxe.erb 

item 6.5 ${space} ${os} 6.5 
# 02.distro.ipxe.erb 

item 6.5 ${space} ${os} 6.5 
# 02.distro.ipxe.erb 

item 6.6 ${space} ${os} 6.6 
# 02.distro.ipxe.erb 

item 6.6 ${space} ${os} 6.6 
# 02.distro.ipxe.erb 

item 6.7 ${space} ${os} 6.7 
# 02.distro.ipxe.erb 

item 6.7 ${space} ${os} 6.7 
# 02.distro.ipxe.erb 

item 6.8 ${space} ${os} 6.8 
# 02.distro.ipxe.erb 

item 6.8 ${space} ${os} 6.8 
# 02.distro.ipxe.erb 

item 6.9 ${space} ${os} 6.9 
# 02.distro.ipxe.erb 

item 6.9 ${space} ${os} 6.9 
# 02.distro.ipxe.erb 

item 7.0.1406 ${space} ${os} 7.0.1406 
# 02.distro.ipxe.erb 

item 7.1.1503 ${space} ${os} 7.1.1503 
# 02.distro.ipxe.erb 

item 7.2.1511 ${space} ${os} 7.2.1511 
# 02.distro.ipxe.erb 

item 7.3.1611 ${space} ${os} 7.3.1611 
# 02.distro.ipxe.erb 

item 7.4.1708 ${space} ${os} 7.4.1708 
# 03.footer.distro.ipxe.erb

choose menu || goto centos_exit
echo ${cls}
goto ${menu} ||
iseq ${sigs_enabled} true && goto verify_sigs || goto change_menu

:verify_sigs
imgverify ${menu}.ipxe ${sigs}${menu}.ipxe.sig || goto error
goto change_menu

:change_menu
chain ${menu}.ipxe || goto error
goto centos_menu
=======
=======
>>>>>>> 03dcf6f73f84a91be510289cb2cf94cc05c8db60
item 2.1 ${space} ${os} 2.1 
item 2.1 ${space} ${os} 2.1 
item 3.1 ${space} ${os} 3.1 
item 3.1 ${space} ${os} 3.1 
item 3.3 ${space} ${os} 3.3 
item 3.3 ${space} ${os} 3.3 
item 3.4 ${space} ${os} 3.4 
item 3.4 ${space} ${os} 3.4 
item 3.5 ${space} ${os} 3.5 
item 3.5 ${space} ${os} 3.5 
item 3.6 ${space} ${os} 3.6 
item 3.6 ${space} ${os} 3.6 
item 3.7 ${space} ${os} 3.7 
item 3.7 ${space} ${os} 3.7 
item 3.8 ${space} ${os} 3.8 
item 3.8 ${space} ${os} 3.8 
item 3.9 ${space} ${os} 3.9 
item 3.9 ${space} ${os} 3.9 
item 4.0 ${space} ${os} 4.0 
item 4.0 ${space} ${os} 4.0 
item 4.1 ${space} ${os} 4.1 
item 4.1 ${space} ${os} 4.1 
item 4.2 ${space} ${os} 4.2 
item 4.2 ${space} ${os} 4.2 
item 4.3 ${space} ${os} 4.3 
item 4.3 ${space} ${os} 4.3 
item 4.4 ${space} ${os} 4.4 
item 4.4 ${space} ${os} 4.4 
item 4.5 ${space} ${os} 4.5 
item 4.5 ${space} ${os} 4.5 
item 4.6 ${space} ${os} 4.6 
item 4.6 ${space} ${os} 4.6 
item 4.7 ${space} ${os} 4.7 
item 4.7 ${space} ${os} 4.7 
item 4.8 ${space} ${os} 4.8 
item 4.8 ${space} ${os} 4.8 
item 4.9 ${space} ${os} 4.9 
item 4.9 ${space} ${os} 4.9 
item 5.0 ${space} ${os} 5.0 
item 5.0 ${space} ${os} 5.0 
item 5.1 ${space} ${os} 5.1 
item 5.1 ${space} ${os} 5.1 
item 5.10 ${space} ${os} 5.10 
item 5.10 ${space} ${os} 5.10 
item 5.11 ${space} ${os} 5.11 
item 5.11 ${space} ${os} 5.11 
item 5.2 ${space} ${os} 5.2 
item 5.2 ${space} ${os} 5.2 
item 5.3 ${space} ${os} 5.3 
item 5.3 ${space} ${os} 5.3 
item 5.4 ${space} ${os} 5.4 
item 5.4 ${space} ${os} 5.4 
item 5.5 ${space} ${os} 5.5 
item 5.5 ${space} ${os} 5.5 
item 5.6 ${space} ${os} 5.6 
item 5.6 ${space} ${os} 5.6 
item 5.7 ${space} ${os} 5.7 
item 5.7 ${space} ${os} 5.7 
item 5.8 ${space} ${os} 5.8 
item 5.8 ${space} ${os} 5.8 
item 5.9 ${space} ${os} 5.9 
item 5.9 ${space} ${os} 5.9 
item 6.0 ${space} ${os} 6.0 
item 6.0 ${space} ${os} 6.0 
item 6.1 ${space} ${os} 6.1 
item 6.1 ${space} ${os} 6.1 
item 6.2 ${space} ${os} 6.2 
item 6.2 ${space} ${os} 6.2 
item 6.3 ${space} ${os} 6.3 
item 6.3 ${space} ${os} 6.3 
item 6.4 ${space} ${os} 6.4 
item 6.4 ${space} ${os} 6.4 
item 6.5 ${space} ${os} 6.5 
item 6.5 ${space} ${os} 6.5 
item 6.6 ${space} ${os} 6.6 
item 6.6 ${space} ${os} 6.6 
item 6.7 ${space} ${os} 6.7 
item 6.7 ${space} ${os} 6.7 
item 6.8 ${space} ${os} 6.8 
item 6.8 ${space} ${os} 6.8 
item 6.9 ${space} ${os} 6.9 
item 6.9 ${space} ${os} 6.9 
item 7.0.1406 ${space} ${os} 7.0.1406 
item 7.1.1503 ${space} ${os} 7.1.1503 
item 7.2.1511 ${space} ${os} 7.2.1511 
item 7.3.1611 ${space} ${os} 7.3.1611 
item 7.4.1708 ${space} ${os} 7.4.1708 
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot
<<<<<<< HEAD
>>>>>>> 03dcf6f73f84a91be510289cb2cf94cc05c8db60
=======
>>>>>>> 03dcf6f73f84a91be510289cb2cf94cc05c8db60

:centos_exit
clear menu
exit 0
<<<<<<< HEAD
<<<<<<< HEAD
=======
=======
>>>>>>> 03dcf6f73f84a91be510289cb2cf94cc05c8db60
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0
:older_release
set older_release true
set centos_mirror old-releases.centos.com
echo Setting mirror to ${centos_mirror}
clear centos_version
echo -n Please set enter code name of release: ${} && read centos_version
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/
goto centos_boot_type

:mirrorcfg
set mirrorcfg mirror/suite=${centos_version}
set dir ${centos_base_dir}/dists/${centos_version}/main/installer-${arch_a}/current/images/netboot/

:centos_boot_type
menu ${os} [${centos_version}] Installer
item --gap Install types
item install ${space} Install
item rescue ${space} Rescue Mode
item expert ${space} Expert Install
item preseed ${space} Specify preseed url...
choose --default ${type} type || goto centos
echo ${cls}
goto centos_${type}

:centos_rescue
set install_params rescue/enable=true
goto centos_boot

:centos_expert
set install_params priority=low
goto centos_boot

:centos_preseed
echo -n Specify preseed URL for ${os} ${centos_version}: && read preseedurl
set install_params auto=true priority=critical preseed/url=${preseedurl}
goto centos_boot

:centos_install
:centos_boot
set dir ${dir}${menu}-installer/${arch_a}
imgfree
kernel http://${centos_mirror}/${dir}/linux ${install_params} ${netcfg} ${mirrorcfg} ${console} -- quiet ${params} initrd=initrd.gz
initrd http://${centos_mirror}/${dir}/initrd.gz
echo
echo MD5sums:
md5sum linux initrd.gz
iseq ${img_sigs_enabled} true && iseq ${older_release} true && goto skip_sigs ||
iseq ${img_sigs_enabled} true && goto verify_sigs || goto skip_sigs
:verify_sigs
echo
echo Checking signatures...
imgverify linux ${sigs}${dir}/linux.sig || goto error
imgverify initrd.gz ${sigs}${dir}/initrd.gz.sig || goto error
echo Signatures verified!
echo
:skip_sigs
boot

:centos_exit
clear menu
exit 0

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases

#!ipxe

# Centos Operating System
# 

goto ${menu}

:centos
set os Centos
clear centos_version
menu ${os} - ${arch_a} - Image Sig Checks: [${img_sigs_enabled}]
item --gap Latest Releases
<<<<<<< HEAD
>>>>>>> 03dcf6f73f84a91be510289cb2cf94cc05c8db60
=======
>>>>>>> 03dcf6f73f84a91be510289cb2cf94cc05c8db60
